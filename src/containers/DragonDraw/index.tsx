import React from "react";
import PixelGrid from "../../components/PixelGrid";

const DragonDraw = () => {
  return (
    <div>
      <PixelGrid height={500} width={500} />
    </div>
  );
};

export default DragonDraw;
